[pytest]
markers =
    integration: requires a live running LINE PC (extracts the key from process memory and reads the real .edb). Skipped automatically when LINE is not running. Run the full suite with LINE open to measure the >=90% coverage gate; day-to-day use `pytest -m "not integration"`.
